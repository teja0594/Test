import { NgKolorDirective } from './ng-kolor.directive';

describe('NgKolorDirective', () => {
  it('should create an instance', () => {
    const directive = new NgKolorDirective();
    expect(directive).toBeTruthy();
  });
});
